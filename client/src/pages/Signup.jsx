import { Link } from 'react-router-dom';

const Signup = () => {
    return (
        <div>
            <h1>Sign Up</h1>
            <Link to='/login'>Login Page</Link>
        </div>
    );
};

export default Signup;